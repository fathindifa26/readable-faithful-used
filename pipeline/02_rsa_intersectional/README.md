# 02 — RSA on true intersectional cells

Same RSA test as stage 01, but on genuine two-attribute intersectional
cells (e.g. "Black Protestant") instead of single-attribute proxies —
the actual target this project cares about.

**Finding:** confirms and sharpens finding 1; also surfaces that the pilot
in stage 03 happened to pick the weakest type (RACE×RELIGION, ρ=0.067,
n.s.) while AGE/EDUCATION sit around ρ≈0.50. Output:
`results/02_rsa_intersectional/rsa_intersectional_summary.csv`.
