# raw survey data users regenerate locally (see data/README.md)
data/opinionqa_original/
data/opinionqa_intersectional.csv

# large intermediate arrays regenerated by re-running notebooks.
# The small ones under results/ ARE shipped (see the un-ignore rules below):
# the survey ground-truth distance matrix and the probe predictions, which
# together let most analysis scripts run without a GPU.
*.npz
*.npy
!results/04_fidelity_map/group_real_dist.npy
!results/03_lgroup_pilot/embeddings_pilot.npz
!results/08_probe_causal_dissociation/probe_v2_pred_*.npz

# LaTeX build junk
paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.blg
paper/*.bbl
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/sections/*.aux

# python
__pycache__/
*.pyc
.ipynb_checkpoints/

# OS junk
.DS_Store
Thumbs.db
